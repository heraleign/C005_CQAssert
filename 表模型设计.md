## 二、数据库设计

### 2.1 中间表设计

#### 2.1.1 系统中间表 (dx_mid_system)
```sql
CREATE TABLE dx_mid_system (
  id VARCHAR(100) PRIMARY KEY COMMENT '主键UUID',
  sys_id VARCHAR(255) NOT NULL COMMENT '子系统编码（集团唯一标识）',
  assigned_organization VARCHAR(255) COMMENT '所属单位',
  belong_dept VARCHAR(255) COMMENT '归属部门',
  sys_name VARCHAR(255) NOT NULL COMMENT '系统名称',
  sub_sys_name VARCHAR(255) COMMENT '子系统名称',
  sys_introduct VARCHAR(2000) COMMENT '系统简介',
  sys_classify VARCHAR(255) COMMENT '系统分类',
  contractor_unit VARCHAR(255) COMMENT '承建单位',
  contractor_department VARCHAR(255) COMMENT '承建部门',
  contractor_leader VARCHAR(255) COMMENT '承建负责人姓名',
  contractor_leader_phone VARCHAR(255) COMMENT '承建负责人手机',
  contractor_leader_email VARCHAR(255) COMMENT '承建负责人邮箱',
  construction_vendor VARCHAR(255) COMMENT '承建厂商',
  management_leader VARCHAR(255) COMMENT '系统管理负责人姓名',
  management_leader_phone VARCHAR(255) COMMENT '系统管理负责人联系电话',
  management_leader_email VARCHAR(255) COMMENT '系统管理负责人邮箱',
  operation_unit VARCHAR(255) COMMENT '运营单位',
  operation_department VARCHAR(255) COMMENT '运营部门',
  operation_leader VARCHAR(255) COMMENT '运营负责人姓名',
  operation_leader_phone VARCHAR(255) COMMENT '运营负责人手机',
  operation_leader_email VARCHAR(255) COMMENT '运营负责人邮箱',
  launch_time VARCHAR(255) COMMENT '系统上线时间 yyyyMMdd',
  project_code VARCHAR(255) COMMENT '所属工程项目编码',
  project_name VARCHAR(255) COMMENT '所属工程项目名称',
  contract_code VARCHAR(255) COMMENT '合同编码或自建系统编码',
  sys_func_type VARCHAR(255) COMMENT '系统功能类型 1纯数据2纯功能3数据+功能',
  if_managed VARCHAR(255) COMMENT '是否需要纳管/盘点 0否1是',
  website VARCHAR(255) COMMENT '前台登录地址',
  netins_sys_id VARCHAR(255) COMMENT '网信安系统编码',
  sys_status VARCHAR(255) COMMENT '系统状态 0未上线1在线2已下线',
  record_name VARCHAR(255) COMMENT '系统定级备案名称',
  master_data_code VARCHAR(255) COMMENT '主数据编码',
  external_tag VARCHAR(255) COMMENT '是否对外产数项目 0否1是',

  -- 同步控制字段
  sync_status VARCHAR(10) DEFAULT '0' COMMENT '同步状态 0待同步 1已同步',
  sync_time DATETIME COMMENT '同步时间',
  source_table VARCHAR(50) DEFAULT 'ay_dx_assets_system' COMMENT '来源表',

  -- 稽核字段
  audit_status VARCHAR(10) DEFAULT '0' COMMENT '稽核状态 0待稽核 1稽核中 2通过 3不通过',
  audit_time DATETIME COMMENT '稽核时间',
  audit_message TEXT COMMENT '稽核问题详情JSON',

  -- 上传标记
  upload_flag VARCHAR(10) DEFAULT '1' COMMENT '上传标记 0不上传 1上传',

  -- 操作类型（集团要求）
  oper_type VARCHAR(10) COMMENT '操作类型 0新增 1修改 2删除',
  oper_time VARCHAR(20) COMMENT '更新时间 YYYYMMDDHHMMSS',

  -- 审计字段
  created_by VARCHAR(50) COMMENT '创建人',
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  updated_by VARCHAR(50) COMMENT '修改人',
  updated_time DATETIME ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',

  INDEX idx_sys_id (sys_id),
  INDEX idx_audit_status (audit_status),
  INDEX idx_sync_status (sync_status)
) COMMENT='系统中间表';
```

#### 2.1.2 数据库中间表 (dx_mid_database)
```sql
CREATE TABLE dx_mid_database (
  id VARCHAR(100) PRIMARY KEY,
  dbs_id VARCHAR(255) NOT NULL COMMENT '数据库唯一标识',
  db_type VARCHAR(255) COMMENT '数据库类型',
  db_name VARCHAR(255) COMMENT '数据库/数据库实例',
  db_version VARCHAR(255) COMMENT '数据库版本',
  sys_id VARCHAR(255) NOT NULL COMMENT '归属系统唯一标识',
  db_ip VARCHAR(255) COMMENT '数据库IP',
  db_port VARCHAR(255) COMMENT '数据库端口',

  sync_status VARCHAR(10) DEFAULT '0',
  sync_time DATETIME,
  audit_status VARCHAR(10) DEFAULT '0',
  audit_time DATETIME,
  audit_message TEXT,
  upload_flag VARCHAR(10) DEFAULT '1',
  oper_type VARCHAR(10),
  oper_time VARCHAR(20),

  created_by VARCHAR(50),
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_by VARCHAR(50),
  updated_time DATETIME ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_dbs_id (dbs_id),
  INDEX idx_sys_id (sys_id),
  INDEX idx_audit_status (audit_status)
) COMMENT='数据库中间表';
```

#### 2.1.3 表中间表 (dx_mid_table)
```sql
CREATE TABLE dx_mid_table (
  id VARCHAR(100) PRIMARY KEY,
  tbl_id VARCHAR(255) NOT NULL COMMENT '表的唯一标识',
  table_schema VARCHAR(255) COMMENT '表所属Schema名称',
  table_en_name VARCHAR(255) NOT NULL COMMENT '表英文名',
  table_name VARCHAR(255) COMMENT '表中文名',
  table_introduct TEXT COMMENT '表简介',
  dbs_id VARCHAR(255) NOT NULL COMMENT '数据库唯一标识',
  table_domain VARCHAR(255) COMMENT '表所属主题域分类',
  scenario_tag VARCHAR(255) COMMENT '业务目录（含应用场景标签)',
  lake_data_type VARCHAR(255) COMMENT '湖内/湖外数据 0湖内1湖外',
  in_unit_lakes VARCHAR(255) COMMENT '是否已入各单位湖 0否1是',
  premium_model_in_lake VARCHAR(255) COMMENT '是否各单位湖内精品模型 0否1是',
  uploaded_to_big_lake VARCHAR(255) COMMENT '是否已上传至集团大数据湖 0否1是',
  external_unique_identifier VARCHAR(255) COMMENT '湖外表的唯一标识',
  is_shareable VARCHAR(255) COMMENT '是否可共享 1-6',
  is_shared VARCHAR(255) COMMENT '是否已共享 0否1是',
  sharing_channel VARCHAR(255) COMMENT '共享渠道',
  tech_contact VARCHAR(255) COMMENT '技术联系人',
  tech_contact_phone VARCHAR(255) COMMENT '技术人员电话',
  data_aggregation_method VARCHAR(255) COMMENT '数据汇聚方式',
  data_collection_time VARCHAR(255) COMMENT '数据采集时间',
  aggregation_granularity VARCHAR(255) COMMENT '汇聚粒度',
  is_incremental_or_full VARCHAR(255) COMMENT '增量/全量',
  storage_period VARCHAR(255) COMMENT '存储周期',
  reference_count VARCHAR(255) COMMENT '引用次数',
  sub_count VARCHAR(255) COMMENT '订阅次数',
  col_count VARCHAR(255) COMMENT '收藏次数',
  access_count VARCHAR(255) COMMENT '访问次数',
  table_level VARCHAR(255) COMMENT '表的分级 1-4',
  tabtable_category VARCHAR(255) COMMENT '表的分类',
  sample_data LONGTEXT COMMENT '样例数据JSON',
  data_rows VARCHAR(255) COMMENT '数据行数',
  process_end_time VARCHAR(255) COMMENT '模型数据期望到达时间',
  partition_field VARCHAR(255) COMMENT '分区',
  sub_partition VARCHAR(255) COMMENT '子分区',
  storage_path VARCHAR(255) COMMENT '目录',
  earliest_billmonths VARCHAR(255) COMMENT '最早账期',
  latest_billmonths VARCHAR(255) COMMENT '最晚账期',
  whole_billmonths LONGTEXT COMMENT '全部账期',
  layer_level VARCHAR(255) COMMENT '层级',
  business_domain VARCHAR(255) COMMENT '业务域',
  source_system VARCHAR(255) COMMENT '数据来源系统',
  create_time VARCHAR(255) COMMENT '创建时间',
  is_partitioned VARCHAR(255) COMMENT '是否分区表 0否1是',
  data_quality VARCHAR(255) COMMENT '数据质量',
  industry_catalog VARCHAR(255) COMMENT '行业数据目录',
  industry_expert VARCHAR(255) COMMENT '行业专家',
  group_gather_tbname VARCHAR(255) COMMENT '集团数据湖采集接口层表名',

  sync_status VARCHAR(10) DEFAULT '0',
  sync_time DATETIME,
  audit_status VARCHAR(10) DEFAULT '0',
  audit_time DATETIME,
  audit_message TEXT,
  upload_flag VARCHAR(10) DEFAULT '1',
  oper_type VARCHAR(10),
  oper_time VARCHAR(20),

  created_by VARCHAR(50),
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_by VARCHAR(50),
  updated_time DATETIME ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_tbl_id (tbl_id),
  INDEX idx_dbs_id (dbs_id),
  INDEX idx_audit_status (audit_status)
) COMMENT='表中间表';
```

#### 2.1.4 字段中间表 (dx_mid_field)
```sql
CREATE TABLE dx_mid_field (
  id VARCHAR(100) PRIMARY KEY,
  tbl_id VARCHAR(255) NOT NULL COMMENT '表的唯一标识',
  field_en_name VARCHAR(255) NOT NULL COMMENT '字段英文名',
  field_name VARCHAR(255) COMMENT '字段中文名',
  field_type VARCHAR(500) COMMENT '字段类型',
  field_length VARCHAR(255) COMMENT '字段长度',
  field_desc VARCHAR(2000) COMMENT '字段描述',
  process_caliber_desc VARCHAR(255) COMMENT '加工口径说明',
  is_primary_key VARCHAR(255) COMMENT '是否主键 0否1是',
  is_foreign_key VARCHAR(255) COMMENT '是否外键 0否1是',
  is_shareable VARCHAR(255) COMMENT '是否可共享 0否1是',
  field_category VARCHAR(255) NOT NULL COMMENT '字段分类',
  sensitivity_level VARCHAR(255) NOT NULL COMMENT '敏感级别 1-4',
  sensitive_field_elements VARCHAR(255) COMMENT '字段的敏感元素',
  is_desensitized VARCHAR(255) COMMENT '是否需脱敏 0否1是',
  value_definition VARCHAR(255) COMMENT '取值定义',
  mdm_type VARCHAR(255) COMMENT '引用主数据类型',
  mdm_field VARCHAR(255) COMMENT '引用主数据字段',

  sync_status VARCHAR(10) DEFAULT '0',
  sync_time DATETIME,
  audit_status VARCHAR(10) DEFAULT '0',
  audit_time DATETIME,
  audit_message TEXT,
  oper_type VARCHAR(10),
  oper_time VARCHAR(20),

  created_by VARCHAR(50),
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_by VARCHAR(50),
  updated_time DATETIME ON UPDATE CURRENT_TIMESTAMP,

  INDEX idx_tbl_id (tbl_id),
  INDEX idx_audit_status (audit_status)
) COMMENT='字段中间表';
```

### 2.2 中间结果表（带账期）

```sql
CREATE TABLE dx_result_system (
  id VARCHAR(100) PRIMARY KEY,
  bill_month VARCHAR(10) NOT NULL COMMENT '账期 YYYYMM',
  -- ... 包含所有中间表字段
  result_status VARCHAR(10) DEFAULT '0' COMMENT '结果状态 0待审核 1已审核 2已上传',
  result_time DATETIME COMMENT '生成时间',
  INDEX idx_bill_month (bill_month),
  INDEX idx_result_status (result_status)
) COMMENT='系统中间结果表';

-- 其他资产类型同理创建：dx_result_database, dx_result_table, dx_result_field...
```

### 2.3 稽核记录表

```sql
CREATE TABLE dx_audit_record (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  audit_batch_id VARCHAR(50) NOT NULL COMMENT '稽核批次ID',
  asset_type VARCHAR(20) NOT NULL COMMENT '资产类型 system/database/table/field',
  audit_scope VARCHAR(20) COMMENT '稽核范围 single/batch/all',
  sys_id VARCHAR(255) COMMENT '系统ID（按系统稽核时填写）',
  audit_status VARCHAR(10) COMMENT '稽核状态 0进行中 1完成',
  total_count INT COMMENT '总记录数',
  pass_count INT COMMENT '通过数',
  fail_count INT COMMENT '不通过数',
  audit_start_time DATETIME COMMENT '稽核开始时间',
  audit_end_time DATETIME COMMENT '稽核结束时间',
  operator VARCHAR(50) COMMENT '操作人',
  INDEX idx_batch_id (audit_batch_id),
  INDEX idx_sys_id (sys_id)
) COMMENT='稽核批次记录表';

CREATE TABLE dx_audit_detail (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  audit_batch_id VARCHAR(50) NOT NULL COMMENT '稽核批次ID',
  asset_type VARCHAR(20) COMMENT '资产类型',
  asset_id VARCHAR(255) COMMENT '资产ID',
  field_name VARCHAR(100) COMMENT '问题字段',
  audit_rule VARCHAR(100) COMMENT '稽核规则编码',
  audit_message TEXT COMMENT '问题描述',
  severity VARCHAR(10) COMMENT '严重级别 error/warning',
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  INDEX idx_batch_id (audit_batch_id),
  INDEX idx_asset_id (asset_id)
) COMMENT='稽核问题明细表';
```

### 2.4 操作日志表

```sql
CREATE TABLE dx_oper_log (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  log_type VARCHAR(20) COMMENT '日志类型 sync/edit/audit/upload',
  asset_type VARCHAR(20) COMMENT '资产类型',
  asset_id VARCHAR(255) COMMENT '资产ID',
  oper_action VARCHAR(50) COMMENT '操作动作',
  before_value LONGTEXT COMMENT '修改前值JSON',
  after_value LONGTEXT COMMENT '修改后值JSON',
  operator VARCHAR(50) COMMENT '操作人',
  oper_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  remark TEXT COMMENT '备注',
  INDEX idx_asset_id (asset_id),
  INDEX idx_oper_time (oper_time),
  INDEX idx_log_type (log_type)
) COMMENT='操作日志表';
```

### 2.5 同步规则表

```sql
CREATE TABLE dx_sync_rule (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  rule_name VARCHAR(100) NOT NULL COMMENT '规则名称',
  rule_type VARCHAR(20) COMMENT '规则类型 default_value/auto_generate',
  asset_type VARCHAR(20) COMMENT '资产类型',
  target_field VARCHAR(100) COMMENT '目标字段',
  source_field VARCHAR(100) COMMENT '源字段',
  rule_expression TEXT COMMENT '规则表达式',
  rule_priority INT DEFAULT 0 COMMENT '优先级',
  enabled VARCHAR(10) DEFAULT '1' COMMENT '是否启用 0否1是',
  created_by VARCHAR(50),
  created_time DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_by VARCHAR(50),
  updated_time DATETIME ON UPDATE CURRENT_TIMESTAMP
) COMMENT='同步规则配置表';
```

### 2.6 不上传标记表

```sql
CREATE TABLE dx_exclude_mark (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  asset_type VARCHAR(20) NOT NULL COMMENT '资产类型 database/table',
  asset_id VARCHAR(255) NOT NULL COMMENT '资产ID',
  sys_id VARCHAR(255) COMMENT '所属系统ID',
  exclude_reason TEXT COMMENT '不上传原因',
  operator VARCHAR(50) COMMENT '操作人',
  oper_time DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '操作时间',
  INDEX idx_asset_id (asset_id),
  INDEX idx_sys_id (sys_id)
) COMMENT='不上传标记表';
```